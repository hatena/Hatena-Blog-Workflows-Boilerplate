name: push when publishing from draft

on:
  pull_request:
    branches:
      - main
    types: [closed]
    paths:
      - "draft_entries/**"

jobs:
  push-when-publishing-from-draft:
    uses: hatena/hatenablog-workflows/.github/workflows/push-when-publishing-from-draft.yaml@cc16ec795c064ac5a15ac5d4ae536053d7fdce2b # v2.0.12
    with:
      BLOG_DOMAIN: ${{ vars.BLOG_DOMAIN }}
    secrets:
      OWNER_API_KEY: ${{ secrets.OWNER_API_KEY }}
