name: push draft to hatena blog

on:
  pull_request:
    paths:
      - "draft_entries/**"

jobs:
  push-draft:
    uses: hatena/hatenablog-workflows/.github/workflows/push-draft.yaml@c07e5aa45846c02b1f297e28bc519da50081a31c # v2.0.8
    secrets:
      OWNER_API_KEY: ${{ secrets.OWNER_API_KEY }}
